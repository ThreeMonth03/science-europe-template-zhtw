# 0.3.45 配對來源與原生輸出整合

這次是實際英文／中文來源的配對升版，不是另一份手工修改的原型套件。
英文 commit：`8c624359fcbc59ff5b364feba1fadab54a2090bf`。
乾淨中文建置 commit：`79e49267c5430d9ea053f89e71a4c9b079f556d8`。
轉換工具未改版，仍為 `25e339fbdfb1d20796471055790aad6a4226b6ed`。

五個共用 Jinja、兩個共用 Word 資產及兩條 Word 轉換流程已整合。
中文由原有轉換流程產生，762 組既有中英譯文全部保留，只增加五組。
檢核仍為預設模式，未命名資料的中性原始編號保留；沒有另維護中文 Jinja 分支。

## 原生結果

三個公開合成案例 × 中英 × 檢核／提交，共 12 組：

- 真正 0.3.45 ZIP 產出 36 份 HTML／PDF／DOCX 與 12 份 LibreOffice 預覽。
- 封存原型 ZIP 原檔另於同一天重跑 36 份文件與 12 份預覽。
- 每組 HTML 全文字節一致；每份 DOCX 所有內部檔案一致，只有經驗證的
  建立／修改時間允許不同。全文、標點、作者段落、URL、零值及表格內容保留。
- 原生 PDF 與 Word 預覽的逐頁文字座標、像素全部一致，未遮罩日期或整頁。
- 四張長文案例中的短表格均完整同頁，沒有孤立的重複表頭。
- 中文長文提交 PDF 維持 7 頁；已回答與局部漏填的中文提交 PDF 均為 4 頁。

| 長文短表格 | 檢核版 Word 預覽 | 提交版 Word 預覽 |
| --- | ---: | ---: |
| 英文 | 第 6 頁完整同頁 | 第 5 頁完整同頁 |
| 中文 | 第 8 頁完整同頁 | 第 6 頁完整同頁 |

另有 3,312 組實際套件的結構比較通過。來源、格式 UUID、ZIP 成員、資產及可重現
時間戳經逐項驗證，才允許精確還原到 0.3.44，繼續跑舊版 544 組與更早的檢查。
英文 231 個測試及 TDK 驗證、中文整套 443 個原有／新增來源測試均已通過；
本紀錄另外由 `tests/test_submission_reading_native.py` 重算原生結果。

## 跨日對照

前輪日期是 2026-09-21，本輪日期是 2026-09-22，直接逐像素比較必然不同。
因此重跑相同舊 ZIP 作同日控制，不修改文件、不忽略日期區塊或整頁。
`diagnostics/cross-day-date.json` 只記錄唯讀診斷，不是用來放寬原生驗收的投影。
正式對照使用 `control/` 與 `native/` 的原生檔案。

## 範圍與回復

`preview-ci/` 保留預覽建置階段的歷史關卡證據，manifest 如實標示 preview。
其 ZIP 與後續乾淨 candidate 完全相同；這不是完整 GitHub Actions 通過的宣告。
Actions 狀態應查對應 commit 的執行紀錄，不能從封存檔案數推定。

僅操作既有 localhost 測試環境與公開合成問卷。兩輪四個測試模板已清理、ZIP 備份
保留、容量限制恢復原值、stock worker 恢復、四個所屬服務停止。未動正式 DSW。

合成問卷刻意包含 `N/A`、`尚待補充`、重複名稱與看似系統提示的使用者原文，用以
證明不會誤刪答案；空白題目也刻意保留。這些不是供實際送件使用的示範答案。

本輪完成此範圍的配對來源與原生回歸，不代表任意專案都已可直接送件、全域提示
開關完成、Science Europe／國科會全面驗收，或 Microsoft Word 跨版本視覺驗收。
畫面抽查見 `visual/`；下一步應另做代表性完整專案的整份中英內容與視覺校讀。
