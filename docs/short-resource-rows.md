# 0.3.42：多筆預算的短列連頁

英文共用來源新增 PDF 專用的短列判定，中文沿用既有轉換流程。
748 組譯文、題目、自由回答、CSS、字型及 Word 來源不變。
這是 `fix/profile-pagination` 的成對實驗版本，不是正式發布或正式站部署。

## 修正與證據

[16 組原生前後對照與可下載成品](../reviews/2026-09-18-short-resource-rows/README.md)。
英文八筆預算第 7 列不再拆到第 8／9 頁；中文檢核版第 6 列、提交預覽第 7 列
也不再拆到第 7／8 頁。每列的名稱、用途、金額與經費來源都保留並同頁；
續頁表頭仍在。所有案例頁數及字型不變，Word 正文、樣式、連結與正文分頁不變。

來源只新增 `src/pdf/short-resource-rows.html.j2`，並在
`budgetReading.render` 沒有長列展開時呼叫。只考慮 4–32 列的普通預算表；
各欄有文字量、段落數及不可斷行字串長度上限，且只接受明列的簡單標記。
符合條件的列加 `break-inside: avoid`，不綁整張表、不重寫任何儲存格。
已知漏填提示與 0 金額也必須通過相同上限；不是把未知當作已回答。

三列以下沿用原規則；長回答、複雜內容、不支援的結構維持原行為。
同一表格若已有長列展開，這個新規則整表略過。未聲稱混合長短列、超過
32 列或任意真實專案已全面驗收；下一輪應先補這些代表性壓力案例。

## 版本與回歸

- 英文 0.3.42：`200fac5239051e1a877493c7d52c3a199964c718`。
- 中文來源鎖 0.3.42：`3551f091fd7916a58dca34235b9cc65771154984`。
- 工具仍固定 `25e339fbdfb1d20796471055790aad6a4226b6ed`。
- 中英文各 120 組 escaping／結構檢查、28 組實際 print／screen 排版對照。
- 完整 prepared-source hashes 精確逆向還原 0.3.41，再跑原有歷史 gates；
  不搬移基準，不略過原來的題目或翻譯檢查。
- 48 份原生 HTML／PDF／DOCX，加 16 份 LibreOffice Word 預覽。

檢核／提交是同一模板內的輸出格式，不另切長期分支。中文鎖定英文 commit，
版本以套件版號與來源 SHA 綁定；證據及 QA commit 不另改模板文字或另造中文來源。

目前仍為提交「預覽」：本輪沒有擴大漏填提示的關閉範圍，也未做 Microsoft Word
實機、ODT／LaTeX 或國科會正式繳交驗收。
